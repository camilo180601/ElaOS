# Artefactos de build de live-build (pesados / regenerables)
*.iso
*.img
*.log
*.packages
*.files
*.contents
.build/
cache/
chroot/
chroot.files
binary/
binary.*
local/
.stage/

# Archivos de config que regenera `auto/config` en cada build
config/bootstrap
config/binary
config/chroot
config/common
config/source

# El arsenal Kali se genera en cada build (ver build.sh / CI)
config/package-lists/kali-tools.list.chroot
